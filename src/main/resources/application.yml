logging.level.org.springframework: INFO
server.port: 8199

management.security.enabled: false    # only needed in Spring boot 1.x
management.endpoints.web.exposure.include: "*"  # only needed in Spring boot 2.0.0.RELEASE

spring:
  datasource:
    url: 'jdbc:h2:tcp://localhost:9092/./testdb'
    username: test
    password: test
    driverClassName: org.h2.Driver